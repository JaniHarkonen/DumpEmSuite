import TabDefault from "./TabDefault";

export default class TabFundamental extends TabDefault {
    constructor(host = null, id = -1, name = "", jsx = <></>) {
        super(host, id, name, jsx);
    }
}